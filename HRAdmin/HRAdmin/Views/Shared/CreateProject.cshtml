@using HRAdmin.Models
@model ProjectViewModel

@using (Html.BeginForm("CreateProject", "ManageEmployee"))
{

    @Html.LabelFor(x => x.Name)
    @Html.TextBoxFor(model => model.Name, new { Style = "width:150;", required = "required" })
                       
    
    <input type="submit" value="Create">
}
